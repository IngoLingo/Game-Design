using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyStates_Scr : MonoBehaviour
{
   /* public enum KeyStatusStates { Clean, Infected, Off };
    public enum KeyModeState { ClosedLock, OpenLock };

    public GameObject myDoor;
    public GameObject objectGeo;

    public KeyStatusStates myKeyStatusSt;
    public KeyModeState myKeyModeSt;

    void Update()
    {
        if ((myKeyModeSt == KeyModeState.OpenLock) &&
            myKeyStatusSt != KeyStatusStates.Off)
        {
            myDoor.GetComponent<DoorStates_Scr>().myDoorStatusSt = DoorStates_Scr.DoorStatusStates.Open;
        }

        if ((myKeyModeSt == KeyModeState.ClosedLock) &&
            myKeyStatusSt != KeyStatusStates.Off)
        {
            myDoor.GetComponent<DoorStates_Scr>().myDoorStatusSt = DoorStates_Scr.DoorStatusStates.Close;
        }

        //Colors
        if (myKeyStatusSt == KeyStatusStates.Clean)
        {
            objectGeo.GetComponent<Renderer>().material.color = new Color(1f, 0.5f, 0.5f);
        }
        else if (myKeyStatusSt == KeyStatusStates.Infected)
        {
            objectGeo.GetComponent<Renderer>().material.color = new Color(0.5f, 1f, 0.5f);
        }
        else if (myKeyStatusSt == KeyStatusStates.Off)
        {
            objectGeo.GetComponent<Renderer>().material.color = new Color(0.5f, 0.5f, 0.5f);
        }
    }*/
}
